using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System.IO;
using System.Linq;
using System;


[ExecuteInEditMode]
public class DemoOptitrackPlayer : DemoRecorderBase
{
    // Start is called from the DemoRecorderBase class, and since we do not need to modify it in 
    // any way, it doesn't even need to be explicitly called in this class
    // Start is called before the first frame update
    // void Start()
    // {
    //     
    // }
    
    // Update is called from the DemoRecorderBase class, and since we do not need to modify it in 
    // any way, it doesn't even need to be explicitly called in this class
    // Update is called once per frame
    // void Update()
    // {
    // }
}
